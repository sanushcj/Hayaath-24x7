import 'package:flutter/material.dart';

class NewestTab extends StatelessWidget {
  const NewestTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Newest'),);
  }
}
